<%= form_with(model: pelicula) do |form| %>
  <% if pelicula.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pelicula.errors.count, "error") %> prohibited this pelicula from being saved:</h2>

      <ul>
        <% pelicula.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :director, style: "display: block" %>
    <%= form.text_field :director %>
  </div>

  <div>
    <%= form.label :año, style: "display: block" %>
    <%= form.date_field :año %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
